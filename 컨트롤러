@ 컨트롤러 만들어서 url 매핑하기

웹 브라우저와 같은 클라이언트의 요청이 발생
> 서버 역할을 하는 스프링 부트가 응답
> url이 스프링 부트에 매핑되어 있어야 함
> 먼저 컨트롤러를 만들어야 함

@Controller 어노테이션 적용 > 클래스가 스프링부트의 컨트롤러가 됨
@GetMapping 어노테이션 > 요청된 URL 매핑 담당

브라우저가 URL 요청 > 스프링 부터는 요청페이지와 매핑되는 메서드를 컨트롤러에서 찾아 실행

